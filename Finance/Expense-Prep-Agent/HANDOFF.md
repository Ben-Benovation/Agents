# Handoff — Expense-Prep-Agent (המשך בסשן חדש)

> ⏩ **הכי עדכני (2026-07-15):** התיוק כמעט הושלם — ראה **`work/runs/2026-07-15-handoff-filing.md`**.
> הסיווג והתיוק של ~40 קבצי ההוצאה ל‑Drive בוצעו; נותר להשלים את הגיליון הראשי + לדחוף לריפו.
> נעצר בגלל באג ברמת השיחה בקונקטור Google Drive (`requires approval` בכל קריאה). התחל מהמסמך ההוא.

> מסמך העברה לסשן/צ'אט חדש. נכתב כי בסשן המקורי היה באג חוזר בערוץ ההרשאות
> (קונקטור Gmail הופיע `enabledInChat: false` ונפל שוב ושוב למרות שהיה מופעל).
> סשן נקי אמור לפתור זאת.

## ✅ עודכן 2026-07-14 — הרצת מאי+יוני בוצעה
ההרצה הראשונה הושלמה בסשן זה. ראה `work/runs/2026-07-may-june.md` לפירוט מלא (21 מסמכים,
גיליון ראשי, תרחישי Make). הדוסייה עלה ל‑v0.2 (גיליון ראשי אחד, שם קובץ עם סוג מסמך, קטגוריות
`זמני`+`תרומה`, socilay=איליי). מה שנשאר: הפעלת תרחישי ה‑Make, Meta, וספקי כרטיס בתיבת beneliya22.

## מצב נוכחי (מה כבר נעשה)
- הסוכן **גויס ותועד** במלואו — ראה `AGENT.md`, `dossier.md`, `system-prompt.md`,
  `work/expected-suppliers.md`. הכל ב‑PR #1 (`ben-benovation/agents`), ענף
  `claude/financial-expense-manager-agent-4ejwcq`.
- סקיל הגיוס `agent-hiring` הותקן ב‑`.claude/skills/agent-hiring/`.
- **קונקטורים:** Google Drive — מחובר ומופעל. Gmail — מחובר, אך בסשן המקורי סבל
  מהבאג. תיבה מחוברת: `info@benovation.co.il` בלבד.

## המשימה הבאה (להריץ בסשן החדש)
**ניסיון איתור על `info@benovation.co.il` לתקופה מאי + יוני 2026** — איתור וסיווג בלבד
בשלב ראשון (להראות לבן איך הסיווג נראה), ואז, לאחר אישורו, כתיבה לדרייב.

### אופן החיפוש (חשוב — שני מסלולים)
1. **מיילים עם קובץ מצורף** — חשבונית/קבלה כ‑PDF/תמונה.
2. **מיילים ללא קובץ** — שהתוכן עצמו הוא הקבלה/חשבונית (בן הדגיש שזה קורה).
   לחפש לפי מילות מפתח: `חשבונית`, `חשבונית מס`, `קבלה`, `קבלה על תשלום`, `תשלום`,
   `invoice`, `receipt`, וכן לפי שמות הספקים ב‑`work/expected-suppliers.md`.
- **לסנן רעש:** `has:attachment` לבדו מחזיר מאות זימוני יומן (`.ics`). להחריג זימוני
  יומן ולמקד בחשבוניות/קבלות/ספקים. (בבדיקה הראשונית מתוך 201 תוצאות, כמעט כולן היו
  זימוני Google Calendar; הוצאה אמיתית שנמצאה: קבלת ארנונה מ‑`citypay@onecity.co.il`.)

### יעד ופלט ב‑Drive
- תיקיית שורש: `11VmuJl-GhRPUEHds-KUxnwQRXMvXLb9K`
- לכל חודש: תת‑תיקייה `חודש שנה` (למשל `יוני 2026`) → בתוכה גיליון
  `סיכום הוצאות - <חודש שנה>` + קבצי המסמכים בשם `קטגוריה - ספק - חודש`.
- כתיבה = הוספה בלבד, ללא מחיקה/דריסה. שום outbound.

### עמודות הגיליון
`timestamp | period | category | vendor | expense_month | amount | invoice_no | source |
drive_file_link | status | confidence | rationale | ben_correction | correction_reason`
סטטוסים: `נדרש לעדכן תיעוד` · `לא נמצא` · `low-confidence`.

## פתוחים / קאווייטים
- **תיבה שנייה `beneliya22@gmail.com`:** טרם חוברה. MCP מאמת חשבון אחד בכל פעם →
  הפתרון המומלץ: העברה אוטומטית מ‑beneliya22 ל‑info עם label, או תרחיש Make. להחליט עם בן.
- **משיכת קובץ מצורף:** `get_thread` (FULL_CONTENT) מחזיר `attachment_ids`; יש לוודא
  בפועל אם ניתן למשוך את בייטי הקובץ ולהעלותם לדרייב, ואם לא — להשלים דרך Make.
- **הערות תקופה (מאי‑יוני):** ראה `dossier.md §9` (ארנונה שולמה מראש, זיכוי מים 111.12₪
  ביוני, עובדים=אפריל, שיווק מתחיל ממאי, אף ספק שנתי לא מתחדש במאי/יוני).

## פרומפט פתיחה מומלץ לסשן החדש
> "המשך את הפעלת Expense-Prep-Agent לפי `Finance/Expense-Prep-Agent/` (dossier +
> system-prompt + expected-suppliers). הרץ איתור וסיווג על תיבת `info@benovation.co.il`
> לתקופה מאי+יוני 2026, בשני המסלולים (מצורף + תוכן הודעה), סנן זימוני יומן, והצג לי
> טבלת ממצאים לפני כתיבה לדרייב."
