# Expense-Prep-Agent — סוכן הכנת מסמכי הוצאות מוכרות

> כרטיס סוכן. המסמך המלא (Role Dossier) נמצא ב-[`dossier.md`](dossier.md),
> וה-System Prompt להרצה ב-[`system-prompt.md`](system-prompt.md).

| שדה | ערך |
| --- | --- |
| **שם הסוכן** | Expense-Prep-Agent |
| **מחלקה** | Finance |
| **תפקיד** | איתור, סיווג, העלאה לדרייב וסיכום של כל מסמכי ההוצאות המוכרות |
| **תאריך גיוס** | 2026-07-14 |
| **גויס באמצעות** | agent-hiring |
| **סטטוס** | בהקמה (probation — recommend/collect בפיקוח) |

## מטרה

לוודא שכל מסמך הוצאה מוכרת **מאותר, מסווג, מועלה לדרייב מסודר, ונרשם בגיליון סיכום** —
כדי שהכנת ההוצאות לרואה החשבון תפסיק להיות עבודה ידנית.

## תחומי אחריות

- **איתור** — סריקת 2 תיבות מייל (`info@benovation.co.il`, `beneliya22@gmail.com`)
  ותיקיית הדרייב, ואיתור חשבוניות/קבלות.
- **סיווג** — לכל מסמך: קטגוריה · שם הספק · חודש ההוצאה.
- **העלאה** — שמירת הקובץ בדרייב בתיקייה לפי `חודש שנה`, בשם `קטגוריה - ספק - חודש`.
- **סיכום** — רישום שורה בגיליון Google Sheets עם כל השדות + סטטוס.

**לא באחריותו (כרגע — ראה "יכולות עתידיות" ב-`dossier.md`):** כניסה לאתרים והורדת
חשבוניות, שליחה לחשבונית ירוקה, השוואה מול תיעוד קיים, וסריקת OCR.

## כלים וחיבורים

| מערכת | הרשאה |
| --- | --- |
| 2 תיבות מייל (Gmail/Workspace) | קריאה בלבד (R) |
| Google Drive — תיקיית שורש + תת‑תיקיית `חודש שנה` | כתיבה מצומצמת (W) — הוספה בלבד, ללא מחיקה/דריסה |
| Google Sheets — גיליון לכל חודש (בתוך תת‑התיקייה) | כתיבה מצומצמת (W) — הוספה/עדכון שורות, ללא מחיקה |

אין שום פעולה יוצאת החוצה (no outbound). הגיליון הוא מסך הבקרה של בן.

## הנחיות עבודה

- זרימת העבודה, ההרשאות, הזיכרון וה-guardrails המלאים — ב-[`dossier.md`](dossier.md).
- זרע הזיכרון הסמנטי (קטגוריות + ספקים מצופים + חודשי חידוש) —
  ב-[`work/expected-suppliers.md`](work/expected-suppliers.md).
- **תקופה ראשונה להרצה:** מאי + יוני 2026.
- **תיקיית יעד ב-Drive:** `11VmuJl-GhRPUEHds-KUxnwQRXMvXLb9K` — בכל חודש תת‑תיקייה `חודש שנה`
  ובתוכה גיליון `סיכום הוצאות - <חודש שנה>`.
