# Handoff — אלידע (סוכן עדכוני קהילת וואטסאפ)

מסמך המשכיות למעבר לסשן חדש. עודכן: 2026-08-14.

## עדכון סשן 2026-08-14 — ריצת שישי: תמונות חסומות (רגרסיה ברשת)
- ✅ **הריצה עצמה הצליחה במלואה:** 3 טיוטות מאומתות ומדורגות נכתבו ללוח `5100685502` (item + Doc לכל אחת, כולל עמודות קטגוריה/יום יעד/תאריך/מקור/ביטחון), והתראה נשלחה לבן. מועמד רביעי (שדרוג Gemini Notebook Pro) נבדק ונפסל בגלל טריות — הפיצ׳ר המרכזי (cloud computer) התפרסם באמצע יולי, מעל 14 יום, גם אם "השלמת גלגול" דווחה ב-5.8 במקורות משניים.
- ⛔ **תמונות נכשלו — רגרסיה ברשת, לא בעיית מפתח:** גם `generativelanguage.googleapis.com` (Gemini, ל-SOP-3b) וגם `pypi.org`/`files.pythonhosted.org` (להתקנת Pillow, שכבר לא מותקן כברירת מחדל בסביבה) החזירו **403 policy-denial** מהפרוקסי (`gateway answered 403 to CONNECT`), למרות ש-`GEMINI_API_KEY` מוגדר ותקין וש-HANDOFF הקודם דיווח שזה נבדק ועובד. כלומר מדיניות הרשת של ה-Routine המתוזמן **השתנתה/נסוגה** מאז 2026-07-20 והפסיקה לאפשר את שני הדומיינים האלה.
- 🔧 **פעולה נדרשת מבן:** לפתוח מחדש במדיניות הרשת של הסביבה/ה-Routine: (1) `generativelanguage.googleapis.com` (Gemini image), (2) `pypi.org` + `files.pythonhosted.org` (pip install Pillow/python-bidi). אפשר גם לשקול Docker image מוכן מראש עם Pillow מותקן, כדי לא להיות תלויים ב-pypi בכל ריצה.
- ℹ️ נהלי SOP-3b כן קוראים ל"דלג על התמונה וציין בדיווח" כשזה נכשל — זה מה שנעשה כאן; שום דבר לא הומצא/עוקף.

## מה זה
גיוס העובד הדיגיטלי **אלידע** — מוצא, מאמת וכותב **3–5 טיוטות עדכון בכל ריצה** (ראשון/שלישי/שישי) לקהילת הוואטסאפ של BenoVation, ומגיש אותן במאנדיי לאישור בן. בן מאשר ושולח (שלב 1 ידני; שלב 2 דרך Green API). מטרת-על: לחסוך לבן זמן; אם אין תוכן ששווה — לא מעלים כלום.

## ריפו / Git
- ריפו: **Ben-Benovation/Agents** · ענף: **claude/whatsapp-community-updates-hk7609** · **PR #3** (open, draft).
- תיקיית הסוכן: `Agents/תוכן-וקהילה/אלידע/` — `AGENT.md`, `ROLE-DOSSIER.md`, `sources-seed.md`, `selection-rubric.md`, `monday-setup.md`, `HANDOFF.md`.
- הסקילים הרלוונטיים (גלובליים): `/whatsapp-updates` (כתיבה), `agent-hiring` (עיצוב תיק), `update-config` (הרשאות).

## monday.com
- חשבון: `eliyabnr-company` · משתמש בן: **62725549** · workspace **CRM** `2199900` · תיקייה **עדכוני קהילה** `3199324`.
- **לוח עדכונים** `5100685502` — קבוצות: `topics`=ממתין לאישור, `group_mm5ebsab`=אושר ונשלח, `group_mm5evftj`=ארכיון.
  - עמודות: `color_mm5e3dsk`=קטגוריה (AI/עסקים ושירות/יומיומי) · `color_mm5esd7d`=יום יעד (ראשון/שלישי/שישי) · `date_mm5e42n8`=תאריך יעד · `doc_mm5ekrf2`=הפוסט (Doc, הבית הראשי) · `long_text_mm5eh81`=טיוטת הפוסט (גיבוי) · `file_mm5ex900`=תמונה נלווית · `link_mm5ed2rj`=מקור · `numeric_mm5ept70`=רמת ביטחון 1-5 · `color_mm5e7nhy`=החלטת בן (מאושר לשליחה/ארכיון/לא מתאים בכלל/לא מתאים חלש) · `date_mm5eg3r1`=תאריך הכנה · `color_mm5eka68`=נשלח ב-Green API (ממתין לשליחה/נשלח/תקלה בשליחה) · `button_mm5e2855`=שלח טסט.
- **לוח מקורות** `5100686191` — 26 מקורות זרועים. עמודות: `color_mm5eh5sk`=באקט · `color_mm5ekfzk`=סוג מקור · `link_mm5ehn8n`=קישור · `color_mm5et69k`=עדיפות · `text_mm5egpdg`=הערות.
- פריט לדוגמה קיים: **3096667378** (Claude Cowork) עם Doc.
- כתיבת פוסט: Doc (`create_doc`, location=item, column_id=`doc_mm5ekrf2`, docOwnerIds=["62725549"]) עם הפוסט בתוך **בלוק קוד** (```) לשימור כוכביות → Bold בוואטסאפ.

## תזמון והתראות
- Routine ריצת קצב: **trig_01PZGLG1m7zsZFwSSvvvLC8H** — ראשון/שלישי/שישי בבוקר, סשן טרי, מריץ את כל הנוהל.
- ⚠️ בריצה הראשונה לוודא שלסשן המתוזמן יש גישה ל-monday MCP (המערכת התריעה שאולי אין). אם אין — להגדיר את ה-Routine מממשק ה-Routines של claude.ai. חיפוש רשת זמין לו.
- מנוי PR: הסשן עוקב אחרי PR #3 עם check-in שעתי (send_later) — ממשיך עד מיזוג/סגירה.

## החלטות שסוכמו עם בן
1. מחקר + טיוטות מוכנות (בן מאשר ושולח).
2. 3–5 טיוטות בכל ריצה, מדורגות, כדי שיהיה ממה לבחור.
3. תמהיל: רוב AI/עסקים; יומיומי רק כשבאמת שווה. הרף גובר על הכמות.
4. מקורות: שילוב (הצעתי 26 + בן מוסיף). רוחב = קריטי.
5. Doc במקום long_text; כוכביות ל-Bold.
6. עמודת תאריך יעד בנוסף ל"יום יעד".
7. טסט לפני שליחה: כפתור → Make (ארגון BenoVation) → Green API → **972507582857**.
8. תמונות: יצירה ישירה מול Google AI Studio (Gemini image) בתוך התהליך, העלאה ל-`file_mm5ex900`, **בפלטת הלוגו (כחול/טורקיז — לא כתום)** + תמונת בן מהדרייב במקומות מתאימים. מתכון + פלטה: `brand-and-images.md`.

## עדכון סשן 2026-07-20 #2 — תיקון האוטומציה (monday דרך API)
- 🧩 **שורש הבעיה אותר:** ה-Routine בגרסת fresh-session **לא נושא קונקטורי MCP** (המערכת מאשרת: "this trigger stores no MCP connectors"). לכן שתי ריצות בדיקה כתבו אפס טיוטות — לא עניין הרשאות, פשוט אין monday MCP בסשן אוטומטי. בנוסף, קבצי אלידע לא על main, אז סשן טרי גם לא קרא את נוהל התפקיד.
- ✅ **הפתרון:** עבודה מול monday דרך ה-**REST/GraphQL API עם טוקן** (headless-safe). בן פתח את הרשת (Custom + `api.monday.com`) והוסיף `MONDAY_API_TOKEN` כ-env. **כל פעולות monday נבדקו ואומתו** (קריאת מקורות, יצירת פריט+עמודות, Doc+markdown, העלאת תמונה, התראה, מחיקה) — API-Version **2025-10**. מתכון: `monday-api-recipe.md`.
- 🔁 **Routine חדש עצמאי:** `trig_01TyR5iw48Uv9cjSwj2aJhAS` — פרומפט self-contained (כל המתכון בתוכו, לא תלוי בריפו), monday דרך API, פלטה מתוקנת. הישן (MCP) נמחק.
- ⏸️ **מושהה (disabled)** לבקשת בן עד אימות 100%. ריצת אימות נורתה (session cse_01NppXEAnF131oA55pXojZnC). כשתאושר — להפעיל: `update_trigger(trig_01TyR5iw48Uv9cjSwj2aJhAS, enabled:true)`.
- ✅ **Gemini:** `GEMINI_API_KEY` מוגדר כ-env ומאומת — תמונות עובדות בריצה האוטומטית.

## עדכון סשן 2026-07-20 #1 (handoff-continuation)
- ✅ **תמונות עובדות.** מפתח Gemini התקבל מבן ונבדק — יצירת תמונה מקצה-לקצה הצליחה (מודל `gemini-3.1-flash-image`, פלטת הלוגו). המפתח נשמר **רק כמשתנה סביבה `GEMINI_API_KEY`** (לא ב-git). מתכון מלא: `brand-and-images.md`. `imagen-4.0` חסום (404) — לא להשתמש.
- ✅ **פלטת מותג תוקנה** מכתום שגוי (#DD5326) ל**כחול/טורקיז** מהלוגו האמיתי: נייבי #16255F · כחול #1E75C0 · רויאל #2C5AE0 · טורקיז #38A9BC · לבן.
- ⚠️ **Make עדיין חסום בסביבה הזו.** בן נתן מפתח API של Make (ארגון BenoVation), אבל: (א) מדיניות הרשת של הסביבה **חוסמת את make.com** (הפרוקסי מחזיר 403), אז אי אפשר להשתמש במפתח דרך curl; (ב) חיבור ה-Make MCP מחובר לחשבון אחר (רואה רק Hair Straightening House). **פתרון נדרש מבן:** או לחבר מחדש את קונקטור ה-Make ב-claude.ai לחשבון BenoVation, או לאפשר את make.com במדיניות הרשת של הסביבה.
- 🔄 **ריצת בדיקה נורתה** ידנית (fire_trigger) ב-11:49 — סשן `cse_019ApCE5nhADnkwQ34RG4VnV` — כדי לאמת שלסשן המתוזמן יש גישה ל-monday. לבדוק את התוצאה בלוח 5100685502.

## ⛔ חסום — נכסים/גישה שצריך מבן
1. **Make / Green API:** ראה "עדכון סשן" למעלה — צריך חיבור Make לחשבון BenoVation (קונקטור claude.ai) או פתיחת make.com במדיניות הרשת. אז לבנות: כפתור `button_mm5e2855` → webhook → Green API sendMessage → 972507582857 (טסט) / לקהילה (באישור "מאושר לשליחה").
2. **תמונות:** ✅ מפתח התקבל ועובד (env `GEMINI_API_KEY`). נותר: **קישור לתיקיית הדרייב** עם תמונות בן (לשילוב במקומות מתאימים). צריך גם לוודא שהמפתח מוגדר כ-env בסביבת ה-Routine המתוזמן (ראה step-by-step).
3. **allowlist הרשאות:** כתיבת `.claude/settings.json` נחסמה ע"י מסווג הבטיחות (פעולה רגישה, לא לעקוף). בן צריך להדביק/לאשר ידנית:
```json
{ "permissions": { "allow": [
  "mcp__monday_com","mcp__Make","mcp__github","mcp__Google_Drive",
  "Edit","Write","Read","WebSearch","WebFetch","Skill",
  "Bash(git add:*)","Bash(git commit:*)","Bash(git push:*)",
  "Bash(git pull:*)","Bash(git fetch:*)","Bash(git checkout:*)" ] } }
```

## הצעד הבא המומלץ (לפי עדיפות)
1. לקבל **מפתח Gemini** → להפעיל יצירת תמונות (כבר בריצה של מחר).
2. **גישת BenoVation ב-Make** → לבנות את תרחיש הטסט (Green API → 972507582857).
3. אחרי ריצת מחר: לבדוק את הטיוטות + לוודא שהתזמון עבד מול monday.
4. בן להדביק את ה-allowlist.
